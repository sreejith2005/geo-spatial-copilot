# Frontend Reset

* Frontend intentionally reset
* Backend remains fully functional
* APIs available:
  * GET /health
  * GET /ready
  * POST /analyze
  * POST /retrieve
  * POST /vision/infer
* Frontend rebuild pending
